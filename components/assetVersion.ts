/* بصمة نسخة الوسائط.
   أسماء ملفات الفيديو ثابتة (panel.mp4…)، فحين نُعيد ترميزها ونرفعها يظلّ
   المتصفح (وطبقة التخزين في الاستضافة) يقدّم النسخة القديمة المحفوظة —
   فيبدو كأن شيئاً لم يتغيّر. إلحاق هذه البصمة بالمسار يجعل الرابط جديداً
   فيُجلب الملف من الخادم.

   ⚠️ حدّثها في كل مرّة يُعاد فيها ترميز الفيديوهات أو صور الإطارات. */
export const MEDIA_V = "20260728";

/** يُلحق بصمة النسخة بمسار أصل */
export const v = (path: string) => `${path}?v=${MEDIA_V}`;
